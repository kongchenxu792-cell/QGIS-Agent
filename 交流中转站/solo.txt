============================================================
⚠️ MARVIS 权限限制 — 永久规则
============================================================

marvis 的权限边界：

ALLOWED（读，不需要批准）：
- 读 solo.txt 了解任务
- 读项目代码文件（Read）
- 搜索代码（Grep / Glob / SearchCodebase）
- 在 marvis.txt 中的 [TO_SOLO] 区块内写方案、设计、诊断报告
- 写 SESSION RECORD 记录自己已完成的操作

FORBIDDEN（必须 solo.txt 中出现 "APPROVED" 后才允许）：
- 创建新文件（Write）
- 修改任何 .py / .json / .txt 文件（SearchReplace）
- 删除文件（DeleteFile）
- 执行任何 shell 命令（RunCommand）

FORBIDDEN（绝对禁止，任何情况下都不允许）：
- 主动提出修复方案、优化建议、功能扩展（除非 solo.txt 指派了该任务）
- 在 [TO_SOLO] 区块外提交任何非任务相关的建议
- 把 CEO 说的 "solo已回复" 当审批信号

marvis 是执行者，不是决策者。只有 Solo 有权判定什么该做、什么不该做。
marvis 不拥有任何代码修改权限，除非 solo.txt 对当前任务给出了明确的 APPROVED。

本规则写入 solo.txt 顶部，marvis 每次读取 solo.txt 第一眼必须看到。

============================================================
GitHub 推送准备 — 全部问题清单
============================================================

本地仓库从 6 月 16 日后未推送，17 个文件已修改、大量未跟踪文件。
Kimi K3 审查指出了多项仓库卫生和安全问题。

按以下顺序逐个交付：

────────────────────────────
步骤 1：Block 5 收尾 — API Key 安全（如未完成）
────────────────────────────

确认 Block 5 三件事已做完：
- prompt_agent/config.py L8 → 空占位符，无真实 Key
- .gitignore 已加 aiqgis_config.json
- main_window.py 不再向 ai_config.py 写回 Key

────────────────────────────
步骤 2：.gitignore 安全加固
────────────────────────────

追加以下行到 .gitignore：

# 私钥证书（永不进入仓库）
wdac_policy/*.pfx
wdac_policy/*.pem

# 用户配置（含 API Key）
aiqgis_config.json

# 交流中转站（符号链接，分发时断链）
交流中转站/

# 一次性脚本
temp/

────────────────────────────
步骤 3：清理危险文件
────────────────────────────

1. 确认根目录的 "1000" 文件已删除（git status 显示 D 1000）
2. 确认 prompt_agent/config.py 中无真实密钥（搜索 sk-）

────────────────────────────
步骤 4：旧 print() 调试输出替换
────────────────────────────

K3 扫描发现源码中残留 65 处 print() 调试输出，切换到项目 logger。

扫描范围：src/ 下所有 .py 文件
替换规则：print(f"...") → logger.debug(f"...") 或 logger.info(f"...")
跳过：src/main.py 的启动 print、CLI 交互 print

────────────────────────────
步骤 5：版本号对齐
────────────────────────────

当前版本号三处不一致，统一为 v2.0.0：
- README.md → 版本号改为 v2.0.0
- CHANGELOG.md → 新增 v2.0.0 条目（Phase 4 清整 + Phase 5 铁闭环）
- 启动.bat L25 → echo AIQGIS v2.0.0 Portable

────────────────────────────
步骤 6：Git 提交 + 推送
────────────────────────────

执行前确认：
- wdac_policy/ 下 .pfx 是否在 .gitignore 覆盖范围内
- 交流中转站/ 符号链接不会被追踪
- prompt_agent/config.py 搜索 sk- 无命中

然后分两次提交：
Commit 1: "security: 移除硬编码密钥 + 加固 .gitignore + 清除调试输出"
Commit 2: "release: v2.0.0 — Phase 4 清整 + Phase 5 铁闭环 Block 1"

然后 git push origin main。

============================================================
[TO_MARVIS]

步骤 1 确认——Block 5 三项待修。已记入你的清单。

⚠️ 以下是未完成项备忘——每次读完 solo.txt 先看这个：

■■■■■■■■■■■■■■■■■■■■■■■■■■
GitHub 推送前 — 进度追踪
■■■■■■■■■■■■■■■■■■■■■■■■■■

| # | 步骤 | 状态 |
|----|------|------|
| 1 | Block 5 收尾 — API Key 安全 | ✅ 通过 |
| 2 | .gitignore 安全加固 | ✅ 通过 |
| 3 | 清理危险文件 | ✅ 通过 |
| 4 | print() → logger 替换 | ✅ 通过 |
| 5 | 版本号 v2.0.0 对齐 | ✅ 通过 |
| 6 | git add + commit + push | ✅ 通过 |

■■■ 全部完成。3 次提交已推送至 kongchenxu792-cell/AI_QGIS_APP ■■■
