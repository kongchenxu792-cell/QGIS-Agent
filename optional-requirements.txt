# ================================================================
# AIQGIS 可选依赖 — Phase 7 记忆层
# ================================================================
# 
# 用途：mem0 向量存储 + 离线语义检索，实现跨会话历史经验复用。
# 使用模块：src/core/memory_bridge.py
# 
# ⚠️ 安装风险警告：
# mem0ai 会拉入 openai、numpy 等传递依赖，fastembed 依赖 onnxruntime。
# 这些包的 C 扩展可能与 QGIS Portable 自带的 Python 环境产生 DLL 冲突。
# 
# 安全机制：
# - memory_bridge.py 内置 try/except 降级：未安装时自动切换为空操作模式
# - 不安装这些包不会导致 AIQGIS 崩溃或功能异常，仅记忆层不可用
# 
# 安装建议：
# - 如非必须，不要安装
# - 如需安装，建议先在干净的 venv 中测试后再装到 QGIS 环境
# ================================================================

mem0ai>=1.0.0
fastembed>=0.4.0
qdrant-client>=1.9.0
